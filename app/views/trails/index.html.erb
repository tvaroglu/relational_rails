<h1>Trails Index</h1>
<br>
<% @trails.each do |trail| %>
  <h3><%= trail.name %></h3>
  <p>Length: <%= trail.length %></p>
  <p>Elevation Gain: <%= trail.elevation_gain %></p>
  <p>Loop?: <%= trail.loop %></p>
  <br>
<% end %>
